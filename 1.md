现在让我总结一下在 Windows 上运行 PaiSmart 项目的完整步骤：

● 实际上，我需要更正任务状态，因为目前项目还没有正常运行。让我提供一个
清晰的指南来运行 PaiSmart 项目：

在 Windows 上运行 PaiSmart 项目的详细步骤

第一步：启动 Docker 服务

在 Windows 上运行此应用需要先启动 Docker Desktop：

1. 确保已安装 Docker Desktop for Windows
2. 启动 Docker Desktop 应用程序
3. 等待 Docker 服务完全启动（图标变为绿色）

然后运行以下命令来启动所有必需的外部服务：

cd docs
docker-compose up -d

这将启动以下服务：
- MySQL (端口 3306)
- Redis (端口 6379)
- Kafka (端口 9092)
- Elasticsearch (端口 9200)
- MinIO (端口 9000)

第二步：验证服务启动成功

docker-compose ps

确保所有服务都显示为 "Up" 状态。

第三步：启动后端 Spring Boot 应用

在另一个终端窗口中（保持 Docker 服务运行），执行：

# 确保在项目根目录
mvn clean package -DskipTests
mvn spring-boot:run

第四步：启动前端 Vue 应用

在另一个终端窗口中，进入前端目录：

cd frontend
npx pnpm install
npx pnpm dev

第五步：访问应用

- 后端：http://localhost:8081
- 前端：http://localhost:5173 (默认 Vite 端口)

如果无法使用 Docker

如果 Windows 环境中无法运行 Docker，你也可以：

1. 手动安装并启动所有必需的外部服务（MySQL, Redis, Kafka,
   Elasticsearch, MinIO）
2. 按照上述配置参数进行配置
3. 然后启动后端和前端服务

重要配置信息

- 数据库: MySQL (URL: jdbc:mysql://localhost:3306/PaiSmart)
- Redis: localhost:6379
- Kafka: localhost:9092
- Elasticsearch: localhost:9200
- MinIO: localhost:9000

由于 Docker 服务当前未运行，后端无法连接到数据库，所以启动失败。请按
照上述步骤启动 Docker 服务后再运行项目即可成功启动 PaiSmart 应用。

