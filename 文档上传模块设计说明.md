# 文档上传模块设计说明

> 本文档为初学者编写，详细解释为什么使用当前的工具类和数据结构设计

---

## 一、目录

1. [概述](#一概述)
2. [核心流程](#二核心流程)
3. [工具类设计原因](#三工具类设计原因)
4. [数据结构设计原因](#四数据结构设计原因)
5. [总结](#五总结)

---

## 一、概述

派聪明（PaiSmart）是一个基于 RAG（检索增强生成）技术的 AI 知识管理系统。**文档上传模块**是整个系统的入口，它负责接收用户上传的文档，并将其处理成 AI 可以理解的形式。

这个模块包含多个组件：

| 组件 | 作用 |
|------|------|
| UploadService | 处理文件分片上传和合并 |
| ParseService | 解析文档内容并分割成小块 |
| VectorizationService | 将文本转换成向量（AI 能理解的数字形式） |
| ElasticsearchService | 存储文档内容和向量，支持快速搜索 |
| FileProcessingConsumer | 异步处理文件任务 |

---

## 二、核心流程

```
用户选择文件
    ↓
【前端】计算文件 MD5 + 分成多个分片（每片约 5MB）
    ↓
【UploadController】接收分片上传请求
    ↓
【UploadService】
    ├─ 把每个分片存到 MinIO（对象存储）
    └─ 用 Redis Bitmap 记录哪些分片已上传
    ↓
【UploadService.mergeChunks()】所有分片传完后合并
    ↓
【Kafka】发送消息："文件准备好了，开始处理"
    ↓
【FileProcessingConsumer】消费 Kafka 消息
    ↓
【ParseService】
    ├─ 从 MinIO 下载文件
    ├─ 用 Apache Tika 解析各种格式（PDF/Word/Excel...）
    ├─ 按语义规则分割成小段（chunk）
    └─ 保存到 MySQL (document_vectors 表)
    ↓
【VectorizationService】
    ├─ 读取文本片段
    ├─ 调用 AI 接口生成向量（768 维浮点数数组）
    └─ 存入 Elasticsearch
    ↓
完成！现在可以搜索和问答了
```

---

## 三、工具类设计原因

### 3.1 UploadService - 为什么要用它？

#### 问题背景
大文件上传会遇到这些问题：
- **网络不稳定**：传到 99% 断了，只能重来
- **内存溢出**：一次性加载 1GB 文件到内存会崩溃
- **无法暂停续传**：没有进度控制

#### 解决方案：分片上传 + Redis Bitmap + MinIO

**为什么使用分片上传？**
```
不使用分片              使用分片（每片 5MB）
┌──────────────┐        ┌────┐ ┌────┐ ┌────┐
│   100MB 文件  │        │5MB │ │5MB │ │5MB │ ...
└──────────────┘        └────┘ └────┘ └────┘
     一次上传               可以并行上传

如果失败：              如果第3片失败：
重新传100MB             只需重传第3片（5MB）
耗时：10分钟            耗时：30秒
```

**为什么使用 Redis Bitmap 记录上传状态？**

```java
// Redis 中存储类似这样的位图：
Key: "upload:userId:abc123md5"
Value: 00011110001... (每一位代表一个分片)
         ↑↑↑↑↑      第 4-8 位是 1，表示这些分片已上传
```

对比其他方案：

| 方案 | 内存占用（1000 个分片） | 查询速度 | 为什么选 Bitmap |
|------|---------------------|---------|----------------|
| 用 List 存储已上传索引 | ~40KB | 快 | ❌ 占用大 |
| 用 Set 存储已上传索引 | ~80KB | 快 | ❌ 占用大 |
| **Redis Bitmap** | **~125 字节** | **极快** | **✅ 最节省空间** |
| 每次查数据库 | 取决于数据量 | 慢 | ❌ IO 开销大 |

**为什么使用 MinIO？**

MinIO 是一个**对象存储服务**（类似阿里云 OSS、AWS S3）：

| 存储方式 | 适用场景 | 为什么不适合我们 |
|---------|---------|-----------------|
| 本地文件系统 | 单机小应用 | ❌ 多服务器时文件不共享 |
| 数据库 BLOB | 小文件 | ❌ 大文件拖慢数据库 |
| NFS 网络存储 | 传统文件共享 | ❌ 性能较差，扩展性差 |
| **MinIO 对象存储** | **大量文件存储** | **✅ 高性能、可扩展、支持预签名 URL** |

**核心代码解析：**

```java
// 标记分片已上传
public void markChunkUploaded(String fileMd5, int chunkIndex, String userId) {
    String redisKey = "upload:" + userId + ":" + fileMd5;
    // setBit: 在指定位置设置 1，表示这个分片已上传
    redisTemplate.opsForValue().setBit(redisKey, chunkIndex, true);
}

// 检查分片是否已上传
public boolean isChunkUploaded(String fileMd5, int chunkIndex, String userId) {
    String redisKey = "upload:" + userId + ":" + fileMd5;
    // getBit: 获取指定位置的值，1=已上传，0=未上传
    return redisTemplate.opsForValue().getBit(redisKey, chunkIndex);
}
```

---

### 3.2 ParseService - 为什么要用它？

#### 问题背景
文档格式多样（PDF、Word、Excel、TXT...），如何统一解析？

直接读 PDF 文件的二进制内容：
```
%PDF-1.4
[一堆乱码和二进制数据...]
```
AI 看不懂这种格式！

#### 解决方案：Apache Tika + 流式处理

**为什么使用 Apache Tika？**

Tika 是一个"万能"文档解析库：

| 格式 | Tika 能否解析 | 其他方式难度 |
|------|-------------|------------|
| PDF | ✅ | ❌ 需要复杂解析 |
| Word (.doc/.docx) | ✅ | ❌ DOC 是专有格式 |
| Excel | ✅ | ❌ 需要专门库 |
| PPT | ✅ | ❌ 需要专门库 |
| TXT | ✅ | ✅ 可直接读 |
| HTML | ✅ | ✅ 可用 DOM 解析 |

**一行代码搞定所有格式：**
```java
AutoDetectParser parser = new AutoDetectParser();
parser.parse(inputStream, handler, metadata, context);
// Tika 自动识别文件类型并解析成纯文本
```

**为什么使用流式处理（Streaming）？**

```java
// 错误示范：一次性加载整个文件到内存
String content = new String(files.getBytes()); // OOM!

// 正确做法：边读边处理
private class StreamingContentHandler extends BodyContentHandler {
    private final StringBuilder buffer = new StringBuilder();

    @Override
    public void characters(char[] ch, int start, int length) {
        buffer.append(ch, start, length);
        if (buffer.length() >= parentChunkSize) {
            processParentChunk(); // 达到一定大小就处理一次
            buffer.setLength(0);  // 清空缓冲区
        }
    }
}
```

**内存占用对比：**

| 文件大小 | 一次性加载 | 流式处理 |
|---------|----------|---------|
| 10MB | ~20MB 内存 | ~1MB 内存 |
| 100MB | ~200MB 内存 | ~1MB 内存 |
| 1GB | **程序崩溃** | ~1MB 内存 |

**为什么需要语义分割？**

简单按字数切分的问题：
```
原文："今天天气真好，适合出去玩。小明去了公园，看到了很多花..."

错误切分（严格按 20 字）:
- "今天天气真好，适合出去玩。小" ← 句子被截断
- "明去了公园，看到了很多花..." ← 从中间开始，语义不完整
```

正确切分（按语义边界）:
```
- "今天天气真好，适合出去玩。" ← 完整句子
- "小明去了公园，看到了很多花..." ← 从句子开头
```

**ParseService 中的智能分割逻辑：**

```java
// 1. 先按段落分割
String[] paragraphs = text.split("\n\n+");

// 2. 段落太长则按句子分割
String[] sentences = paragraph.split("(?<=[。！？；])|(?<=[.!?;])\\s+");

// 3. 句子还是太长则用 HanLP 按词分割
List<Term> termList = StandardTokenizer.segment(sentence);
```

---

### 3.3 VectorizationService - 为什么要用它？

#### 问题背景
AI 模型不能直接理解文字，需要将文本转换成数字向量。

**什么是向量化？**

```
文本："我喜欢猫"
     ↓ AI 模型转换
向量：[0.234, -0.567, 0.891, ..., 0.123] (768 个数字)

文本："我不喜欢狗"
     ↓ AI 模型转换
向量：[0.241, -0.552, 0.885, ..., 0.119] (768 个数字)
```

相似的语义会有相似的向量（数字接近）。

**为什么需要独立的服务？**

```
ParseService          VectorizationService         ElasticsearchService
    │                      │                              │
    │ 输出文本片段           │                              │
    │──────────────────────▶│                              │
    │                      │ 调用 AI 接口生成向量             │
    │                      │──────────────────────────────▶│
    │                      │                    存储向量+文本
    │                      │                         │
    │                 CPU 密集型                   IO 密集型
    │                 (等待 AI 接口)                  (写入 ES)
```

分离的好处：
1. **职责单一**：每个服务只做好一件事
2. **易于测试**：可以单独测试向量化功能
3. **易于扩展**：想换 AI 模型只需改这一个服务

---

### 3.4 ElasticsearchService - 为什么要用它？

#### 问题背景
我们需要快速查找"与用户问题相关的文档片段"。

MySQL vs Elasticsearch：

| 需求 | MySQL | Elasticsearch |
|------|-------|---------------|
| 精确匹配（如 ID=123） | ✅ 很快 | ⚠️ 可以但不推荐 |
| 模糊搜索（包含关键词） | ⚠️ 慢，尤其是大数据量 | ✅ 非常快 |
| **语义搜索（相似含义）** | ❌ 不支持 | **✅ 向量相似度搜索** |
| 全文检索 | ⚠️ 需额外配置 | ✅ 原生支持 |

**为什么不用其他向量数据库（如 Milvus、Chroma）？**

| 方案 | 优点 | 缺点 | 我们的选择 |
|------|------|------|-----------|
| Milvus | 专业向量库，性能好 | 需要额外部署和维护一个服务 | ❌ 增加复杂度 |
| Chroma | 简单易用 | 企业级特性较少 | ❌ 不够稳定 |
| **Elasticsearch** | **已有依赖，同时支持全文和向量搜索** | 向量搜索性能略逊于专用库 | **✅ 够用且减少运维成本** |

**关键代码：**

```java
// 批量索引文档到 Elasticsearch
public void bulkIndex(List<EsDocument> documents) {
    // Bulk API 一次性插入多条记录，比单条插入快几十倍
    List<BulkOperation> bulkOperations = documents.stream()
        .map(doc -> BulkOperation.of(op -> op.index(idx -> idx
            .index("knowledge_base")
            .id(doc.getId())
            .document(doc)
        )))
        .toList();

    BulkRequest request = BulkRequest.of(b -> b.operations(bulkOperations));
    esClient.bulk(request);
}
```

---

### 3.5 FileProcessingConsumer - 为什么要用 Kafka 异步处理？

#### 问题背景

同步处理的流程：
```
用户点击上传 → 等待文件传输 → 等待合并 → 等待解析 → 等待向量化 → 返回结果
     （可能需要几分钟甚至十几分钟...用户界面卡住）
```

用户体验很差！

#### 解决方案：Kafka 异步消息队列

```
用户点击上传 → 等待文件传输 → 等待合并 → 立即返回"上传成功"
                                    ↓
                              【Kafka 消息队列】
                                    ↓
                         后台慢慢处理（解析 + 向量化）
```

**为什么不直接用 Spring Schedule 定时任务？**

| 方案 | 可靠性 | 可扩展性 | 为什么不用 |
|------|--------|---------|-----------|
| 数据库轮询 | ❌ 频繁查库 | ❌ 难以扩展 | 效率太低 |
| Spring Schedule | ⚠️ 任务可能丢失 | ❌ 单点故障 | 不可靠 |
| RabbitMQ | ✅ 可靠 | ✅ 可扩展 | ❌ 生态不如 Kafka |
| **Kafka** | **✅ 消息持久化** | **✅ 横向扩展容易** | **✅ 高吞吐量大** |

**Kafka 带来的好处：**

1. **解耦**：上传服务和处理服务互不影响
2. **削峰填谷**：突然大量上传也不会压垮系统
3. **容错重试**：处理失败会自动重试
4. **可扩展**：可以启动多个 Consumer 并行处理

**死信队列机制：**

```
正常消息 → Kafka Topic → Consumer 处理 → 成功 ✓

失败消息 → Kafka Topic → Consumer 处理 → 失败
                                   ↓
                            重试 3 次仍失败
                                   ↓
                          Dead Letter Queue (死信队列)
                                   ↓
                            发送到管理员邮箱告警
```

---

## 四、数据结构设计原因

### 4.1 FileUpload 实体

```java
@Entity
@Table(name = "file_upload")
public class FileUpload {
    private Long id;           // 数据库自增主键
    private String fileMd5;    // 文件指纹（唯一标识）
    private String fileName;   // 原始文件名
    private long totalSize;    // 文件大小
    private int status;        // 0=上传中，1=已完成
    private String userId;     // 上传者
    private String orgTag;     // 组织标签
    private boolean isPublic;  // 是否公开
}
```

**为什么用 MD5 而不是文件名作为唯一标识？**

场景：两个用户上传同名文件

| 方案 | 结果 |
|------|------|
| 用文件名 | ❌ 后上传覆盖先上传的记录 |
| **用 MD5** | **✅ 相同内容只存一份，节省空间** |

**去重功能实现：**
```java
// 上传前计算文件 MD5
String md5 = DigestUtils.md5Hex(fileBytes);

// 检查是否已存在
Optional<FileUpload> existing = repository.findByFileMd5(md5);
if (existing.isPresent()) {
    return "此文件已存在";  // 无需重复上传
}
```

**为什么需要 status 状态字段？**

```
status = 0: 分片上传进行中
         - 不允许删除
         - 不允许搜索

status = 1: 上传完成，等待处理
         - 可以删除
         - 正在后台解析中

status = 2: 处理完成（可以在 code 中定义）
         - 可以搜索
         - 可以用于问答
```

---

### 4.2 ChunkInfo 实体

```java
@Entity
@Table(name = "chunk_info")
public class ChunkInfo {
    private Long id;
    private String fileMd5;     // 属于哪个文件
    private int chunkIndex;     // 第几个分片（用于排序合并）
    private String chunkMd5;    // 分片校验值（验证完整性）
    private String storagePath; // MinIO 中的路径
}
```

**为什么需要单独的分片信息表？**

场景：上传中断后恢复

```
用户上传了 10 个分片中的前 3 个，然后网络断了...

第二天用户重新上传：
1. 查询 chunk_info 表：发现 chunkIndex 0,1,2 已存在
2. 跳过这 3 个分片，从 chunkIndex 3 开始上传
3. 节省流量和时间
```

**为什么需要 chunkMd5？**

防止分片损坏或被篡改：
```java
// 上传时验证
String clientMd5 = calculateMd5(chunk);
String serverMd5 = chunkInfo.getChunkMd5();

if (!clientMd5.equals(serverMd5)) {
    throw new Exception("分片数据不一致，请重新上传");
}
```

---

### 4.3 DocumentVector 实体

```java
@Entity
@Table(name = "document_vectors")
public class DocumentVector {
    private Long vectorId;
    private String fileMd5;     // 来源文件
    private Integer chunkId;    // 文本块序号
    private String textContent; // 实际文本内容
    private String userId;      // 权限控制
    private String orgTag;      // 组织隔离
    private boolean isPublic;   // 公开标记
}
```

**为什么需要 chunkId？**

一个文件会被切分成多个文本块：
```
文件：《Java 编程思想.pdf》 (200 页)
     ↓ ParseService 解析

chunkId = 1: "第一章：Java 语言概述..."
chunkId = 2: "1.1 Java 的历史..."
chunkId = 3: "1.2 Java 的特点..."
...
```

**搜索时的关联：**
```sql
-- 找到相关文件的所有文本块
SELECT * FROM document_vectors
WHERE file_md5 = 'abc123...'
ORDER BY chunk_id ASC;
```

**为什么要存 userId/orgTag/isPublic？**

权限控制示例：
```java
// 用户 A 搜索时，只能看到：
// 1. 自己上传的文件 (user_id = 'A')
// 2. 自己组织的文件 (org_tag = '技术部')
// 3. 公开文件 (is_public = true)

SELECT * FROM document_vectors
WHERE user_id = :userId
   OR org_tag IN (:userOrgTags)
   OR is_public = true;
```

---

### 4.4 EsDocument 实体（Elasticsearch 专用）

```java
public class EsDocument {
    private String id;          // ES 文档 ID
    private String fileMd5;
    private Integer chunkId;
    private String textContent;
    private float[] vector;     // 768 维向量 ⭐
    private String modelVersion;// 向量模型版本
    private String userId;
    private String orgTag;
    private boolean isPublic;
}
```

**为什么和 DocumentVector 分开？**

```
MySQL (DocumentVector)          Elasticsearch (EsDocument)
├─ 关系型存储                    ├─ 搜索引擎优化
├─ 事务支持                      ├─ 全文检索
├─ 结构化查询                    ├─ 向量相似度搜索
└─ 主数据存储                    └─ 搜索索引（可重建）
```

数据流向：
```
ParseService → MySQL (DocumentVector)  ← 主数据，持久化
                     │
                     ↓
        VectorizationService (生成向量)
                     │
                     ↓
          Elasticsearch (EsDocument)  ← 搜索索引
```

**vector 字段为什么是 float[]？**

```java
// AI 模型输出的向量通常是浮点数数组
float[] vector = [
    0.234f, -0.567f, 0.891f,
    0.123f, -0.456f, 0.789f,
    ... // 共 768 个数字
];

// Elasticsearch 中使用 kNN 搜索找相似向量
{
    "knn": {
        "field": "vector",
        "query_vector": [0.234, -0.567, ...],
        "k": 10,  // 返回最相似的 10 个
        "num_candidates": 100
    }
}
```

---

### 4.5 FileProcessingTask 实体（Kafka 消息）

```java
@Data
@AllArgsConstructor
@NoArgsConstructor
public class FileProcessingTask {
    private String fileMd5;
    private String filePath;   // MinIO 访问链接
    private String fileName;
    private String userId;
    private String orgTag;
    private boolean isPublic;
}
```

**为什么是普通 Java 对象而不是实体类？**

Kafka 消息的特点：
- 不需要数据库映射（不是 Entity）
- 需要序列化传输（JSON/Protobuf）
- 简洁即可（不需要复杂关系）

**Lombok 注解的作用：**

```java
@Data           // 自动生成 getter/setter/toString
@AllArgsConstructor  // 全参构造函数
@NoArgsConstructor   // 无参构造函数（反序列化需要）
```

等价于手动写几十行代码。

---

## 五、总结

### 设计原则总结

| 原则 | 体现 | 好处 |
|------|------|------|
| **分而治之** | 大文件分片、长文分段 | 降低复杂度，提高可靠性 |
| **异步处理** | Kafka 消息队列 | 提升用户体验，解耦系统 |
| **职责单一** | 每个 Service 只做一件事 | 易维护、易测试 |
| **数据持久化** | 每一步都记录状态 | 故障恢复、问题排查 |
| **权限隔离** | 用户/组织/公开三层 | 数据安全 |

### 为什么不能用更简单的方案？

| 简化想法 | 实际问题 |
|---------|---------|
| 不分片，直接上传大文件 | ❌ 网络中断要重来、内存溢出 |
| 不用 Kafka，直接调用处理 | ❌ 用户长时间等待、系统易崩溃 |
| 不用 ES，只用 MySQL 搜索 | ❌ 无法做语义搜索、速度慢 |
| 不把文本切块，整篇存向量 | ❌ 搜索精度低、响应不精准 |
| 不用 Redis，全部查数据库 | ❌ 频繁 IO、性能差 |

### 学习建议

如果你是初学者，建议按以下顺序深入理解：

1. **第一周**：搞懂上传流程（分片→合并）
2. **第二周**：理解解析过程（Tika→切块）
3. **第三周**：掌握向量化原理（文本→数字）
4. **第四周**：了解搜索机制（ES 向量检索）

每个模块都可以单独调试和学习，不必一开始就全局理解。

---

> 文档撰写日期：2024 年
> 适用版本：PaiSmart 1.0
