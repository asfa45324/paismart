# PaiSmart (派聪明) 环境配置与部署指南

PaiSmart (派聪明) 是一个企业级 AI 知识管理系统，采用检索增强生成（RAG）技术，提供智能文档处理和检索能力。

## 目录
- [项目概述](#项目概述)
- [环境要求](#环境要求)
- [Docker 部署方式](#docker-部署方式)
- [手动部署方式](#手动部署方式)
- [配置说明](#配置说明)
- [启动与验证](#启动与验证)
- [常见问题](#常见问题)

## 项目概述

派聪明是一个基于现代技术栈的企业级 AI 知识库管理系统，核心技术包括：

**后端技术栈：**
- 框架 : Spring Boot 3.4.2 (Java 17)
- 数据库 : MySQL 8.0
- ORM : Spring Data JPA
- 缓存 : Redis
- 搜索引擎 : Elasticsearch 8.10.0
- 消息队列 : Apache Kafka
- 文件存储 : MinIO
- 文档解析 : Apache Tika
- 安全认证 : Spring Security + JWT
- AI集成 : DeepSeek API/本地 Ollama+豆包 Embedding
- 实时通信 : WebSocket
- 依赖管理 : Maven
- 响应式编程 : WebFlux

**前端技术栈：**
- 框架 : Vue 3 + TypeScript
- 构建工具 : Vite
- UI组件 : Naive UI
- 状态管理 : Pinia
- 路由 : Vue Router
- 样式 : UnoCSS + SCSS
- 图标 : Iconify
- 包管理 : pnpm

## 环境要求

在开始部署之前，请确保您的系统已安装以下软件：

- **Java 17**
- **Maven 3.8.6 或更高版本**
- **Node.js 18.20.0 或更高版本**
- **pnpm 8.7.0 或更高版本**
- **MySQL 8.0**
- **Elasticsearch 8.10.0**
- **MinIO 8.5.12**
- **Kafka 3.2.1**
- **Redis 7.0.11**
- **Docker（推荐，用于运行服务）**

## Docker 部署方式（推荐）

Docker 部署是最简单的方式，可以一键启动所有依赖服务。

### 1. 克隆项目

```bash
git clone <repository-url>
cd PaiSmart-main
```

### 2. 启动依赖服务

使用 Docker Compose 启动所有必需的外部服务：

```bash
# 进入 docs 目录并启动所有服务
cd docs && docker-compose up -d
```

**Docker Compose 包含的服务：**

| 服务 | 端口 | 用户名/密码 | 用途 |
|------|------|-------------|------|
| MySQL | 3306 | root/PaiSmart2025 | 主数据库 |
| MinIO | 19000, 19001 | admin/PaiSmart2025 | 文件存储 |
| Redis | 6379 | -/PaiSmart2025 | 缓存和会话管理 |
| Kafka | 9092, 9093 | - | 消息队列 |
| Elasticsearch | 9200 | elastic/PaiSmart2025 | 搜索引擎 |

**自动创建的 Kafka Topics:**
- `file-processing` - 文件处理队列
- `vectorization` - 向量化处理队列

### 3. 验证服务启动

```bash
# 检查所有容器状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

## 手动部署方式

如果您不想使用 Docker，可以手动安装和配置各个服务。

### 1. 安装 Java 17 和 Maven

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install openjdk-17-jdk maven

# 验证安装
java -version
mvn -version
```

### 2. 安装并启动 MySQL 8.0

```bash
# Ubuntu/Debian
sudo apt install mysql-server
sudo systemctl start mysql
sudo mysql_secure_installation

# 或使用 Docker
docker run -d --name mysql \
  -e MYSQL_ROOT_PASSWORD=PaiSmart2025 \
  -p 3306:3306 \
  mysql:8
```

### 3. 安装并启动 Redis

```bash
# Ubuntu/Debian
sudo apt install redis-server
sudo systemctl start redis

# 或使用 Docker
docker run -d --name redis \
  -p 6379:6379 \
  redis redis-server --requirepass PaiSmart2025
```

### 4. 安装并启动 Elasticsearch

```bash
# 下载并安装 Elasticsearch
wget https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-8.10.0-linux-x86_64.tar.gz
tar -xzf elasticsearch-8.10.0-linux-x86_64.tar.gz
cd elasticsearch-8.10.0

# 安装中文分词插件
bin/elasticsearch-plugin install https://github.com/medcl/elasticsearch-analysis-ik/releases/download/v8.10.0/elasticsearch-analysis-ik-8.10.0.zip

# 启动 Elasticsearch
./bin/elasticsearch
```

### 5. 安装并启动 Kafka

```bash
# 下载并安装 Kafka
wget https://downloads.apache.org/kafka/3.2.1/kafka_2.13-3.2.1.tgz
tar -xzf kafka_2.13-3.2.1.tgz
cd kafka_2.13-3.2.1

# 启动 Zookeeper
bin/zookeeper-server-start.sh config/zookeeper.properties

# 启动 Kafka
bin/kafka-server-start.sh config/server.properties

# 创建必需的主题
bin/kafka-topics.sh --create --topic file-processing --bootstrap-server localhost:9092
bin/kafka-topics.sh --create --topic vectorization --bootstrap-server localhost:9092
```

### 6. 安装并启动 MinIO

```bash
# 下载并安装 MinIO
wget https://dl.min.io/server/minio/release/linux-amd64/minio
chmod +x minio

# 启动 MinIO
export MINIO_ROOT_USER=admin
export MINIO_ROOT_PASSWORD=PaiSmart2025
./minio server /data --console-address ":19001" -address ":19000"
```

## 后端启动

### 1. 配置数据库

确保 MySQL 数据库已创建并配置，PaiSmart 会自动创建表结构。

### 2. 配置 AI API 密钥

在 `application.yml` 中配置以下环境变量：

```bash
export DEEPSEEK_API_KEY=your_deepseek_api_key
export EMBEDDING_API_KEY=your_embedding_api_key
export JWT_SECRET_KEY=your_jwt_secret_key
```

### 3. 启动后端服务

```bash
# 方法1: 使用 Maven
mvn spring-boot:run

# 方法2: 使用 Maven 构建后运行 JAR
mvn clean package
java -jar target/smart-pai-1.0.0.jar

# 方法3: 使用特定配置文件
mvn spring-boot:run -Dspring-boot.run.profiles=docker
```

### 4. 后端测试

```bash
# 运行单元测试
mvn test

# 运行特定测试
mvn test -Dtest=UserServiceTest
```

## 前端启动

### 1. 安装前端依赖

```bash
cd frontend

# 安装 pnpm（如果未安装）
npm install -g pnpm

# 安装项目依赖
pnpm install
```

### 2. 启动前端开发服务器

```bash
# 启动开发服务器
pnpm run dev

# 构建生产版本
pnpm run build

# 预览生产构建
pnpm run preview

# 类型检查
pnpm run typecheck

# 代码校验
pnpm run lint
```

### 3. 前端开发环境配置

前端配置文件位于 `frontend/` 目录：

- `vite.config.ts` - Vite 构建配置
- `tsconfig.json` - TypeScript 配置
- `pnpm-workspace.yaml` - 工作区配置

## 配置说明

### 主要配置文件

**后端配置文件：**
- `application.yml` - 主配置文件
- `application-dev.yml` - 开发环境配置
- `application-docker.yml` - Docker部署配置

**前端配置文件：**
- `vite.config.ts` - 构建配置
- `tsconfig.json` - TypeScript配置

### 数据库配置示例

```yaml
# application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pai_smart?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: PaiSmart2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
```

### Redis 配置示例

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: PaiSmart2025
    database: 0
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
```

### Elasticsearch 配置示例

```yaml
elasticsearch:
  host: localhost
  port: 9200
  username: elastic
  password: PaiSmart2025
```

### MinIO 配置示例

```yaml
minio:
  endpoint: http://localhost:19000
  access-key: admin
  secret-key: PaiSmart2025
  bucket-name: pai-smart
```

### 消息队列配置

```yaml
# Kafka 配置
spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: pai_smart_group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
```

## 启动与验证

### 1. 验证所有服务是否启动

```bash
# 检查端口占用
netstat -tlnp | grep -E ':(3306|6379|9200|9092|19000)'

# 或使用 ss 命令
ss -tlnp | grep -E ':(3306|6379|9200|9092|19000)'
```

### 2. 后端服务启动验证

启动后端服务后，访问以下端点验证：

```bash
# 健康检查端点
curl http://localhost:8080/actuator/health

# API 文档端点
curl http://localhost:8080/swagger-ui.html
```

### 3. 前端启动验证

启动前端后，访问：

- 前端界面：http://localhost:3000
- 确认前端能够连接到后端 API

### 4. 功能测试

部署完成后，您可以测试以下核心功能：

1. **用户注册/登录** - 验证身份验证系统
2. **文档上传** - 上传 PDF、Word、Excel、图片等
3. **文档处理** - 验证文档解析和向量化
4. **聊天功能** - 与 AI 机器人交互并检索上传的文档

## 常见问题

### 1. 服务启动失败

**问题：** `Could not connect to MySQL database`
**解决方案：**
- 检查 MySQL 服务是否启动
- 验证用户名和密码是否正确
- 确认数据库是否存在

**问题：** `Connection refused: no further information`
**解决方案：**
- 检查相关服务（Redis、Elasticsearch、MinIO）是否启动
- 确认端口配置是否正确

### 2. 内存不足问题

**问题：** `java.lang.OutOfMemoryError`
**解决方案：**
- 增加 JVM 堆内存：`-Xms4g -Xmx4g`
- 增加 Elasticsearch 内存：在 docker-compose 中调整 `ES_JAVA_OPTS`

### 3. 前端构建问题

**问题：** `Module not found` 或依赖安装失败
**解决方案：**
- 清除缓存：`pnpm store prune`
- 重新安装：`pnpm install`

### 4. Elasticsearch 插件安装问题

如果 Elasticsearch 中文分词插件安装失败：

```bash
# 手动安装 IK 分词器
cd /usr/share/elasticsearch/plugins
sudo mkdir analysis-ik
sudo curl -L -o /tmp/ik.zip https://github.com/medcl/elasticsearch-analysis-ik/releases/download/v8.10.0/elasticsearch-analysis-ik-8.10.0.zip
sudo unzip /tmp/ik.zip -d analysis-ik/
sudo chown -R elasticsearch:elasticsearch analysis-ik
```

### 5. 文件上传问题

**问题：** 上传大文件时出现超时或错误
**解决方案：**
- 在配置文件中增加文件上传限制
- 配置 MinIO 的上传限制

### 6. AI API 服务问题

**问题：** AI 响应失败或超时
**解决方案：**
- 检查 API 密钥是否正确配置
- 验证网络连接和 API 服务状态
- 调整请求超时时间

## 性能优化建议

### 数据库优化
- 定期清理历史数据
- 优化数据库索引
- 配置连接池参数

### Elasticsearch 优化
- 调整分片和副本数量
- 优化内存配置
- 定期清理旧索引

### 系统监控
- 配置日志收集和监控
- 设置系统资源监控
- 配置错误追踪系统

## 安全考虑

- 使用强密码保护数据库、Redis、MinIO
- 配置 HTTPS 加密通信
- 定期更新依赖库
- 验证用户上传的文件类型和大小
- 实施适当的访问控制和权限管理

## 生产环境部署

对于生产环境，建议：

1. 使用反向代理（如 Nginx）提供 HTTPS
2. 配置负载均衡
3. 设置数据库主从复制
4. 配置备份策略
5. 实施监控和告警系统